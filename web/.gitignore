# Next.js build output
/.next/
/out/
next-env.d.ts

# Dependencies
/node_modules/

# Environment — NEVER commit real values. See .env.example for the shape.
.env
.env.local
.env.*.local
tsconfig.tsbuildinfo


# OpenNext build output (opennextjs-cloudflare build) — regenerated every build
/.open-next/

# wrangler's local state (wrangler dev) — never source
/.wrangler/
