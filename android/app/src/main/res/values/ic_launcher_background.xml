<?xml version="1.0" encoding="utf-8"?>
<resources>
    <!-- White, per explicit request — the previous value (#313332) was
         sampled directly from logo.png's own dark corner background;
         reverted to white so the adaptive icon's background layer no
         longer matches that dark corner. The foreground layer (logo.png)
         itself is unchanged. -->
    <color name="ic_launcher_background">#FFFFFF</color>
</resources>