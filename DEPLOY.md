# نشر FPVARABIC

كل إعدادات النشر مكتوبة داخل المستودع — في `vercel.json` و`.github/workflows/`.
لا يوجد شيء تضبطه في أي لوحة تحكّم: لا مجلّد جذر، ولا أمر بناء، ولا اختيار إطار عمل.

يبقى **قيمة واحدة** لا يستطيع أحد غيرك إضافتها، ولن تكون في المستودع أبداً.

---

## لماذا لا أستطيع أنا إتمام النشر

بيئة التنفيذ التي أعمل فيها ترفض الاتصال بكل مزوّدي الاستضافة — Vercel وNetlify
وCloudflare وngrok وغيرها — بردّ **403 من بوّابة الخروج**، وهي سياسة شبكة للمؤسسة
لا خللاً. وخدمات Google مفتوحة و`firebase` مثبَّت، لكن النشر إليها يحتاج تسجيل دخول
تفاعلياً عبر المتصفّح أو مفتاح حساب خدمة — وكلاهما بيدك أنت.

هذا ليس عائقاً هندسياً بل **حدّ أمني**: الرمز الذي يسمح بالنشر إلى حسابك يجب أن يأتي
من صاحب الحساب. لو استطعتُ أنا الحصول عليه، لاستطاع غيري.

---

## الطريقة الأولى: خطوتان، بلا أي أدوات على جهازك

بعدها يصبح كل دفع إلى الفرع نشراً تلقائياً — إلى الأبد، بلا تدخّل منك.

### ١. أنشئ رمزاً

<https://vercel.com/account/tokens> ← **Create Token** ← انسخه.

### ٢. ألصقه في المستودع

`Settings` ← `Secrets and variables` ← `Actions` ← **New repository secret**

| | |
|---|---|
| **Name** | `VERCEL_TOKEN` |
| **Value** | الرمز الذي نسخته |

انتهى. اذهب إلى تبويب **Actions** ← **Deploy** ← **Run workflow**.
الرابط يظهر في ملخّص التشغيل خلال دقيقتين تقريباً.

للترقية إلى نطاق الإنتاج لاحقاً: نفس الزر، مع تفعيل **production**.

---

## الطريقة الثانية: استيراد بنقرة، بلا رمز

<https://vercel.com/new> ← استورد `melyanneahmed-rgb/fpv-arabic-app` ← اختر الفرع
`claude/web-platform-foundation` ← **Deploy**.

`vercel.json` في جذر المستودع يخبر Vercel بكل شيء، فلا تحتاج ضبط
Root Directory ولا Build Command.

### إن كان المشروع مضبوطاً على Root Directory = `web`

هذا الوضع يعمل أيضاً، لكن بشرط واحد. حين يكون الجذر هو `web/`، يقرأ Vercel
`web/vercel.json` ويتجاهل الذي في جذر المستودع، ويُثبّت الاعتماديات **داخل
`web/` وحدها**. والنواة المشتركة في `../src` تستورد حزماً (firebase و
browser-image-compression وlucide-react) لا تُحلّ إلا من `node_modules` جذر
المستودع — لأن استيراداً داخل `src/` يبحث صعوداً من `src/`، ولا يمرّ بـ
`web/node_modules` إطلاقاً. لذلك يحمل `web/vercel.json` أمر تثبيت يُثبّت مانيفست
الجذر أولاً ثم مانيفست `web`. بدونه يفشل البناء عند
«Module not found: Can't resolve 'firebase/storage'».

لا تُضِف تلك الحزم إلى `web/package.json` لإسكات الخطأ: ذلك يخلق نسخة ثانية من
Firebase، والسبب مشروح في رأس `web/next.config.ts`.

---

## الطريقة الثالثة: على جهازك

```bash
git clone https://github.com/melyanneahmed-rgb/fpv-arabic-app.git
cd fpv-arabic-app
git checkout claude/web-platform-foundation
npm install
npm install --prefix web
npm run web:build
npm --prefix web run start      # http://localhost:3100
```

من هاتفك على نفس الشبكة: `http://<عنوان-حاسوبك-المحلي>:3100`

---

## ماذا يعمل بلا أي متغيّرات بيئة

الموسوعة، والبحث، والقاموس، والتشخيص، ومراكز البرامج، ومشروعك، والمتجر بكل
أقسامه وصفحات منتجاته، والسلّة، والإعدادات، واتصل بنا، وحول المنصّة.

كلّها تقرأ من النواة المشتركة داخل الحزمة — لا شبكة ولا قاعدة بيانات.

## ماذا يحتاج Firebase

| الوظيفة | المتغيّر |
|---|---|
| تسجيل الدخول | `NEXT_PUBLIC_FIREBASE_*` (ستّة، عامة وغير سرّية) |
| المجتمع · لوحة الإدارة · الطلبات | `FIREBASE_PROJECT_ID` · `FIREBASE_CLIENT_EMAIL` · `FIREBASE_PRIVATE_KEY` |

الثلاثة الأخيرة **مفاتيح رئيسية تتجاوز كل قواعد Firestore وStorage**. أضفها في
Vercel ← Settings ← Environment Variables، ولا تضعها في المستودع أبداً.
راجع `web/.env.example` للأسماء.

**بدونها الموقع يعمل ويقول بوضوح أي وظيفة معطّلة ولماذا** — لا يفشل بصمت.

## النطاق الرسمي

`fpvarabic.com` مكتوب في `src/data/brand.ts` كنطاق قانوني للمنصّة، ويُستعمل في
الروابط الأساسية وخريطة الموقع.

**النشر لا يربطه.** الربط قرارك، ويتمّ من Vercel ← Settings ← Domains بعد شراء
النطاق. وحتى تربطه، كل نشر يُعلن نفسه `noindex` تلقائياً — انظر
`web/lib/siteOrigin.ts` — فلا تتنافس نسخة تجريبية مع الموقع الرسمي في نتائج البحث.
